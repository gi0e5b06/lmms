if(LMMS_HAVE_CARLA)
	ADD_DEFINITIONS(-DCARLA_PLUGIN_EFFECTRACK -DCARLA_PLUGIN_EFFECT)
	INCLUDE(BuildPlugin)
	INCLUDE_DIRECTORIES(${CARLA_INCLUDE_DIRS} "${CMAKE_CURRENT_SOURCE_DIR}/../carlabase")
	LINK_DIRECTORIES("${CMAKE_CURRENT_BINARY_DIR}/../carlabase"
                          ${CARLA_LIBRARY_DIRS})
	LINK_LIBRARIES(carlabase)
	BUILD_PLUGIN(carlaeffectrack carlaeffectrack.cpp EMBEDDED_RESOURCES logo.png)
endif(LMMS_HAVE_CARLA)
